{{> html_head }}
<div class="status-message" role="status">
    <span class="eyebrow">404</span>
    <h1>{{ not_found_title }}</h1>
    <p>{{ not_found_message }}</p>
    <a class="mdl-button mdl-button--raised mdl-button--colored" href="{{ baseUrlRelative }}">{{ home }}</a>
</div>
{{> html_tail }}
